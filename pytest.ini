[pytest]
log_cli = true
log_cli_level = DEBUG
# Restrict test collection to the project test directories to avoid
# import-name collisions between files with the same basename.
testpaths =
	server/tests
	data/tests
